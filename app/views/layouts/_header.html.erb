<nav class="navbar navbar-expand-lg navbar-light bg-custom shadow" id="header">
    <div class="container">
    	<button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#sidebar" aria-controls="sidebar" aria-expanded="false" aria-label="Toggle navigation">
        		<span class="navbar-toggler-icon"></span>
  		</button>
  		
  		<h1 class="navbar-brand">MinhaEmpresa</h1>

  		<div class="collapse navbar-collapse">
      		<ul class="navbar-nav ml-auto">
      			<% if current_user.admin? %>
		          <%= content_tag(:li, :class=> current_class?(users_path)){
		              link_to "Usuários |", users_path, class: " nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(departments_path)){
		              link_to "Departamentos |", departments_path, class: " nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(companies_path)){
		              link_to "Empresa |", companies_path, class: " nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(sectors_path)){
		              link_to "Setores |", sectors_path, class: "nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(service_types_path)){
		              link_to "Tipo de Serviços |", service_types_path, class: "nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(service_statuses_path)){
		              link_to "Serviços Status |", service_statuses_path, class: "nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(roles_path)){
		              link_to "Nivel de Acesso |", roles_path, class: "nav-link"
		          }%>
		          <%= content_tag(:li, :class=> current_class?(tools_path)){
		              link_to "Funções do Sistema |", tools_path, class: "nav-link"
		          }%>
			    <% end %>
      			<% if logged_in? %>
	                <li class="nav-item dropdown">
	                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
	                      	<span>
			                    <% if current_user.picture? %>
			                        <%= image_tag current_user.picture.url, :width=>"50px"%>
			                    <%end%>
			                </span>
	                    </a>
	                    <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
	                       <%= link_to current_user.name, current_user, class: "dropdown-item" %>
	                      <div class="dropdown-divider"></div>
	                       <%= link_to "Sair", logout_path, class: "dropdown-item", method: :delete%>
	                    </div>
	                </li>
	            <% end %>   
	      </ul>
  		</div>
  		</div>
</nav>